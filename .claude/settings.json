{
  "//": "Agent-Hooks fuer Claude Code / agent hooks for Claude Code. Siehe Lektion 18. Feuern beim Arbeiten mit Claude Code in diesem Repo sofort -- kein Preview-Schalter noetig (anders als der Copilot-Agent-Hook in .github/agents/logic.agent.md).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "node scripts/pre-tool-guard.mjs" }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "node scripts/i18n-hook.mjs" }]
      }
    ]
  }
}
